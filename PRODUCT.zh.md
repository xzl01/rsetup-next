# 产品

<!-- impeccable:product-schema 1 -->

## 平台

web

## 技术栈

已委派并确认：一个由 `clap` CLI、`ratatui` TUI、`axum` 本地
API/Web 应用以及可选的 Tauri 桌面外壳共享的 Rust 控制内核。
浏览器与桌面 GUI 使用同一前端。

## 用户

主要用户是直接在一块基于 Linux 的板卡上工作的
SBC 所有者、开发者或管理员。他们需要理解设备、
修改系统与硬件设置，并在不记忆板卡专属命令的情况下，
从常见配置问题中恢复。

集群运营者是未来的目标用户。首个版本刻意采用本地优先
（local-first），而核心 API 与设备身份模型必须为日后的
远程节点留出空间。

## 产品目标

rsetup 是一站式 SBC 控制中心。它将设备可观测性、配置、维护、
硬件控制与引导式操作，统一在三个协同的入口之后：
CLI、TUI 和 GUI。

成功意味着用户能够识别板卡状态、找到合适的操作、理解其风险、
审慎地执行它，并从适配其环境的任意界面
看到可审计的结果。

## 定位

与通用的 Linux 仪表盘不同，rsetup 理解 SBC 特有概念，
例如 device-tree overlay、启动介质与 bootloader、
GPIO 与 LED、热策略、板卡身份，以及恢复敏感操作。
每个界面都是同一类型化操作目录之上的视图，
而非彼此独立的脚本集合。

## 运行环境

- 本地 Debian 或 Ubuntu SBC 安装，包括无头（headless）系统。
- 交互式终端会话、SSH 会话、本地浏览器或桌面
  WebView。
- 部分操作需要 root 权限，并可能影响可引导性。
- 硬件可用性因板卡、内核、已启用的 overlay 和
  外接外设而异。

## 能力与约束

- 首个实现管理一个本地设备，其 Web 服务默认
  绑定到 loopback。
- CLI、TUI、浏览器 GUI 与 Tauri 桌面外壳共享同一个
  Rust 领域模型和操作注册表。
- 英文和简体中文在 CLI、TUI、浏览器 GUI 与 Tauri 中
  都是一等展示语言。机器可读的 JSON 与 HTTP 载荷保持
  语言中立，并保留稳定的标识符。
- 新的控制平面在运行时不得调用或依赖旧版
  命令；允许不兼容的内部变更。
- 在 Linux 暴露数据之处，只读检查必须无需
  提权即可工作。
- 变更类和恢复敏感的操作必须展示影响、前置条件和
  确认要求。任意 shell 执行不是产品
  功能。
- 在非 SBC 主机上的开发使用显式的合成数据和 dry-run
  操作结果。合成状态绝不可被呈现为真实硬件证据。
- 远程多设备管理是悬而未决的未来决策，
  不属于首次交付范围。

## 品牌承诺

保留 `rsetup` 名称与 Radxa/SBC 技术气质。语气应当
简洁、冷静、面向运维，并明确说明风险。没有当前
证据时，不得暗示硬件支持或执行成功。

控制中心使用 Soft UI 视觉体系：浅石板色基底、暖白色
表面、低饱和度靛蓝作为主操作色、宽裕的间距、大圆角
形态，以及柔和着色的阴影。它应当给人亲切、可触碰的
感觉，同时不能让危险操作显得无害；风险继续通过显式
标签、影响文案与确认关卡来表述。

## 现有依据

上游项目历史仍然可用于行为对比。工作树中仅
包含新的 Rust 控制平面、共享界面资产
及其打包内容。

除既有的 rsetup/Radxa 关联之外，不存在已批准的用户
研究、集群需求、生产遥测或视觉识别资产。

## 产品原则

- 一个类型化控制平面，多个界面。
- 在提供操作之前，先让设备状态清晰可读。
- 把危险操作当作引导式流程，而不是按钮。
- 适配检测到的能力，而不是展示不可能的控件。
- 清晰区分合成、计划中、运行中、成功与失败状态。

## 无障碍与包容

Web GUI 必须支持键盘导航、保持清晰的焦点状态、
尊重减少动态效果与对比度偏好，并避免将颜色作为
唯一的状态信号。英文与简体中文布局必须容忍桌面
与窄移动端宽度下的文本扩展，更新文档语言与无障碍
标签，并对未知的提供方文案安全回退。CLI 与 TUI
输出必须在没有真彩色支持的情况下依然易于理解；中文
输出需要终端字体具备 CJK 字形覆盖。
